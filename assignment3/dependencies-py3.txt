# LIST YOUR PYTHON3 PACKAGES HERE
# NOTE: only place non-Duckietown libraries here; pin versions only if necessary
#
# Currently no extra pip packages. No dt_apriltags / apriltag here — detection is done
# on the robot by the standard Duckietown perception pipeline, not inside this image.
